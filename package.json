{
  "name": "utools-json-unwrapper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build:release": "node scripts/build-release.mjs"
  },
  "engines": { "node": ">=18" }
}
